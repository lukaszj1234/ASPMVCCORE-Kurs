
@model IEnumerable<Product>

@foreach (var item in Model)
{
    <div>
        <h3>
            @item.Name
            @item.Description
        </h3>
        <h4> @item.Price.ToString("c")</h4>
</div>
}

